DISCORD_BOT_TOKEN=

# Optional: if true, /rallydm will attempt to DM only online members.
# Requires enabling "Presence Intent" in the Discord Developer Portal AND in code (we do).
DM_ONLY_ONLINE=false

# Optional: delay between DMs (helps be gentle with rate limits)
DM_DELAY_SECONDS=1.2
